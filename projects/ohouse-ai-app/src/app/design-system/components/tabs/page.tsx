'use client';

import { ComingSoon } from '../ComingSoon';

export default function TabsPage() {
  return (
    <ComingSoon
      title="Tabs Component"
      description="Tabbed interface component for organizing content into separate sections. Supports keyboard navigation, active states, and custom styling."
      category="Components"
    />
  );
}
