'use client';

import { ComingSoon } from '../ComingSoon';

export default function ModalPage() {
  return (
    <ComingSoon
      title="Modal Component"
      description="Dialog component for focused user interactions and confirmations. Includes customizable sizes, backdrop dimming, and keyboard navigation support."
      category="Components"
    />
  );
}
