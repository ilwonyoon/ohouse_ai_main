# 프론트엔드 테크 스택 가이드

디자이너를 위한 프론트엔드 개발 환경 구축 가이드입니다.

## 📋 필수 요구사항

### 시스템 요구사항
- **Node.js**: >= 22 버전
- **Package Manager**: Yarn 3.2.0
- **OS**: macOS, Windows, Linux 모두 지원

### Node.js 설치 확인
터미널에서 다음 명령어로 Node.js 버전을 확인하세요:
```bash
node --version
```
버전이 22 이상이 아니라면 [Node.js 공식 사이트](https://nodejs.org/)에서 설치하세요.

### Yarn 설치 확인
터미널에서 다음 명령어로 Yarn 버전을 확인하세요:
```bash
yarn --version
```
Yarn이 설치되어 있지 않다면 다음 명령어로 설치하세요:
```bash
npm install -g yarn
```

## 🛠️ 주요 테크 스택

### 프레임워크 & 라이브러리
- **Next.js**: 15.3.0 (React 프레임워크)
- **React**: 19.0.0 (UI 라이브러리)
- **TypeScript**: 5.x (타입 안정성을 위한 언어)

### 스타일링
- **Emotion**: 11.14.0 (CSS-in-JS 라이브러리)
  - `@emotion/react`: React 통합
  - `@emotion/styled`: styled-components 스타일

### UI 컴포넌트
- **@bucketplace/design-system**: 6.4.3 (내부 디자인 시스템)
- **@bucketplace/ui**: 1.1.0 (내부 UI 컴포넌트)
- **Radix UI**: 접근성 좋은 헤드리스 UI 컴포넌트
  - `@radix-ui/react-checkbox`
  - `@radix-ui/react-toast`
  - `@radix-ui/react-tooltip`
- **Lucide React**: 아이콘 라이브러리

### 상태 관리
- **Jotai**: 2.12.4 (원자 상태 관리)
- **TanStack Query**: 5.72.2 (서버 상태 관리)

### 애니메이션
- **Framer Motion**: 12.7.4 (애니메이션 라이브러리)
- **Lottie React**: 2.4.1 (Lottie 애니메이션)

### 기타 주요 라이브러리
- **i18next**: 25.1.2 (다국어 지원)
- **Embla Carousel**: 8.6.0 (캐러셀 컴포넌트)
- **Fabric.js**: 6.7.1 (Canvas 조작)
- **browser-image-compression**: 2.0.2 (이미지 압축)

## 🚀 환경 구축 방법

### 1. 저장소 클론
```bash
git clone <repository-url>
cd ai-interior-web
```

### 2. Nexus 로그인 (내부 패키지 설치)
내부 패키지(`@bucketplace/*`)를 설치하기 위해 Nexus에 로그인해야 합니다:
```bash
yarn config set @bucketplace:registry https://nexus.co-workerhou.se/repository/npm-private/
yarn login --registry=https://nexus.co-workerhou.se/repository/npm-private/
```

### 3. 의존성 설치
```bash
yarn install
```

### 4. 환경 변수 설정
`.env.local` 파일을 생성하고 필요한 환경 변수를 설정하세요.
(프로젝트에 따라 필요한 환경 변수는 팀에 문의하세요)

### 5. 개발 서버 실행
```bash
yarn dev
```

개발 서버가 실행되면 브라우저에서 `http://localhost:3000/ai-interior`로 접속할 수 있습니다.

## 📁 프로젝트 구조

```
ai-interior-web/
├── src/
│   ├── components/     # 재사용 가능한 컴포넌트
│   ├── pages/          # Next.js 페이지 (라우팅)
│   ├── styles/         # 전역 스타일
│   ├── hooks/          # 커스텀 React 훅
│   ├── utils/          # 유틸리티 함수
│   ├── types/          # TypeScript 타입 정의
│   └── api/            # API 호출 함수
├── public/             # 정적 파일 (이미지, 폰트 등)
├── server/             # Express 서버 코드
└── package.json        # 프로젝트 의존성 및 스크립트
```

## 🎨 디자이너를 위한 주요 파일 위치

### 컴포넌트
- 재사용 가능한 컴포넌트: `src/components/`
- 페이지 컴포넌트: `src/pages/`

### 스타일
- 전역 CSS: `src/styles/global.css`
- Emotion 스타일: 각 컴포넌트 파일 내부

### 이미지 & 에셋
- 정적 이미지: `public/assets/`
- 아이콘: `src/assets/icons/`

### 다국어
- 번역 파일: `public/locales/`
  - 한국어: `public/locales/ko-KR/`
  - 영어: `public/locales/en-US/`

## 📝 주요 스크립트

```bash
# 개발 서버 실행
yarn dev

# 프로덕션 빌드
yarn build

# 타입 체크
yarn typecheck

# 린트 검사
yarn lint

# 린트 자동 수정
yarn lint:fix

# 테스트 실행
yarn test
```

## 🔧 개발 팁

### 1. Hot Reload
개발 서버 실행 중 파일을 수정하면 자동으로 브라우저가 새로고침됩니다.

### 2. TypeScript 타입 체크
컴포넌트를 수정할 때는 TypeScript 타입 오류를 확인하세요:
```bash
yarn typecheck
```

### 3. 스타일링
- Emotion을 사용한 CSS-in-JS 방식으로 스타일을 작성합니다
- 컴포넌트 파일 내부에서 `styled` 또는 `css` prop을 사용합니다

### 4. 컴포넌트 작성 시 주의사항
- TypeScript 타입을 명확히 정의하세요
- 재사용 가능한 컴포넌트는 `src/components/`에 작성하세요
- 페이지는 `src/pages/`에 작성하세요 (Next.js 라우팅)

## 🐛 문제 해결

### 패키지 설치 오류
Nexus 로그인이 제대로 되지 않았을 수 있습니다. 다시 로그인해보세요:
```bash
yarn login --registry=https://nexus.co-workerhou.se/repository/npm-private/
```

### 포트 충돌
다른 포트를 사용하려면 환경 변수에서 포트를 변경하세요.

### 빌드 오류
캐시를 삭제하고 다시 시도해보세요:
```bash
yarn clean
yarn install
yarn dev
```

## 📚 추가 학습 자료

- [Next.js 공식 문서](https://nextjs.org/docs)
- [React 공식 문서](https://react.dev/)
- [TypeScript 공식 문서](https://www.typescriptlang.org/docs/)
- [Emotion 공식 문서](https://emotion.sh/docs/introduction)
- [Framer Motion 공식 문서](https://www.framer.com/motion/)
