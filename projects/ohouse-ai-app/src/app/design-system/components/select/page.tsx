'use client';

import { ComingSoon } from '../ComingSoon';

export default function SelectPage() {
  return (
    <ComingSoon
      title="Select Component"
      description="Dropdown component for selecting from a list of options. Includes variants for single/multiple selection, disabled states, and custom styling."
      category="Components"
    />
  );
}
