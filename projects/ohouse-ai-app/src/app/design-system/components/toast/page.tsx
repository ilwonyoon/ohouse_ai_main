'use client';

import { ComingSoon } from '../ComingSoon';

export default function ToastPage() {
  return (
    <ComingSoon
      title="Toast Component"
      description="Notification component for displaying temporary messages to users. Includes variants for success, warning, error, and info notifications."
      category="Components"
    />
  );
}
