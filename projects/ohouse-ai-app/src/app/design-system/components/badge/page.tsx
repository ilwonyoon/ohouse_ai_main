'use client';

import { ComingSoon } from '../ComingSoon';

export default function BadgePage() {
  return (
    <ComingSoon
      title="Badge Component"
      description="Small component for displaying labels, statuses, and counts. Includes variants for success, warning, error, and info states."
      category="Components"
    />
  );
}
